namespace Jwu.Constants;

/// <summary>
/// Units of time used key "expire" (exp) and "not before" (nbf) values.
/// </summary>
public enum JwtTimeUnit
{
    Year = 0,
    Month = 1,
    Day = 2,
    Hour = 3,
    Minute = 4,
    Second = 5
}
